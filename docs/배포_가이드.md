# 🚀 글로벌 작가 온보딩 페이지 - 배포 가이드

## 📋 목차
1. [GitHub 레포지토리 연동](#1-github-레포지토리-연동)
2. [구글 시트 & 앱스 스크립트 설정](#2-구글-시트--앱스-스크립트-설정)
3. [Vercel 배포](#3-vercel-배포)
4. [환경변수 설정](#4-환경변수-설정)
5. [배포 후 확인사항](#5-배포-후-확인사항)

---

## 1. GitHub 레포지토리 연동

### 대상 레포지토리
- **URL**: https://github.com/pos01204/artists-global-registration
- **상태**: 빈 레포지토리 (초기화 필요)

### Step 1: Git 초기화 및 연동

```bash
# 1. 프로젝트 폴더로 이동
cd "C:\Users\김지훈\Desktop\[Global Business셀] 김지훈\AI 자동화\글로벌 작가 온보딩 페이지"

# 2. Git 초기화
git init

# 3. .gitignore 파일 생성 (아래 내용 참고)

# 4. 모든 파일 스테이징
git add .

# 5. 첫 번째 커밋
git commit -m "feat: 글로벌 작가 온보딩 페이지 초기 구현"

# 6. 메인 브랜치 설정
git branch -M main

# 7. 원격 레포지토리 연결
git remote add origin https://github.com/pos01204/artists-global-registration.git

# 8. 푸시
git push -u origin main
```

### Step 2: .gitignore 파일 생성

```gitignore
# dependencies
/node_modules
/.pnp
.pnp.js
.yarn/install-state.gz

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env*.local
.env

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# IDE
.idea/
.vscode/
*.swp
*.swo
```

---

## 2. 구글 시트 & 앱스 스크립트 설정

### Step 1: 구글 스프레드시트 생성

1. [Google Sheets](https://sheets.google.com) 접속
2. **새 스프레드시트** 생성
3. 스프레드시트 이름: `글로벌 작가 온보딩 데이터`

### Step 2: 시트 구조 설정

**Sheet 1: "전체데이터"** (기본 시트 이름 변경)

| A | B | C | D | E | F | G | H | I | J | K | L | M | N |
|---|---|---|---|---|---|---|---|---|---|---|---|---|---|
| 타임스탬프 | 작가명 | 연락처 | 사업자보유 | 카테고리 | 자격상태 | 식품관심 | 디지털관심 | STEP1 | STEP2 | STEP3 | 퀴즈완료 | 퀴즈점수 | 등록클릭 |

**Sheet 2: "2026확장대기"** (새 시트 추가)

| A | B | C | D | E |
|---|---|---|---|---|
| 타임스탬프 | 작가명 | 연락처 | 관심카테고리 | 비고 |

**Sheet 3: "사업자미등록"** (새 시트 추가)

| A | B | C | D |
|---|---|---|---|
| 타임스탬프 | 작가명 | 연락처 | 관심카테고리 |

### Step 3: Apps Script 설정

1. 스프레드시트에서 **확장 프로그램** → **Apps Script** 클릭
2. 기본 코드 삭제 후 아래 코드 붙여넣기

```javascript
/**
 * 글로벌 작가 온보딩 데이터 수집 스크립트
 * 
 * 시트 구조:
 * - 전체데이터: 모든 온보딩 데이터
 * - 2026확장대기: 식품/디지털 카테고리 관심자
 * - 사업자미등록: 사업자등록번호 미보유자
 */

// 스프레드시트 ID (URL에서 확인 가능)
// https://docs.google.com/spreadsheets/d/[이 부분이 ID]/edit
const SPREADSHEET_ID = SpreadsheetApp.getActiveSpreadsheet().getId();

/**
 * POST 요청 처리 - 온보딩 데이터 저장
 */
function doPost(e) {
  try {
    // 요청 데이터 파싱
    const data = JSON.parse(e.postData.contents);
    
    // 데이터 검증
    if (!data.artistName || !data.phoneNumber) {
      return createResponse(false, '필수 정보가 누락되었습니다.');
    }
    
    // 메인 시트에 저장
    saveToMainSheet(data);
    
    // 자격 상태에 따라 추가 시트에 저장
    if (data.qualificationStatus === 'no_business') {
      saveToNoBusinessSheet(data);
    }
    
    // 2026 확장 카테고리 관심자 저장
    if (data.interested2026Food || data.interested2026Digital) {
      saveTo2026WaitlistSheet(data);
    }
    
    return createResponse(true, '데이터가 성공적으로 저장되었습니다.');
    
  } catch (error) {
    console.error('Error in doPost:', error);
    return createResponse(false, '서버 오류: ' + error.message);
  }
}

/**
 * GET 요청 처리 - 헬스체크
 */
function doGet(e) {
  return createResponse(true, 'Global Artist Onboarding API is running');
}

/**
 * 메인 시트에 데이터 저장
 */
function saveToMainSheet(data) {
  const sheet = getOrCreateSheet('전체데이터');
  
  // 헤더가 없으면 추가
  if (sheet.getLastRow() === 0) {
    sheet.appendRow([
      '타임스탬프', '작가명', '연락처', '사업자보유', '카테고리',
      '자격상태', '식품관심', '디지털관심', 'STEP1완료', 'STEP2완료',
      'STEP3완료', '퀴즈완료', '퀴즈점수', '등록클릭', 'UTM_SOURCE',
      'UTM_MEDIUM', 'UTM_CAMPAIGN'
    ]);
  }
  
  // 데이터 행 추가
  sheet.appendRow([
    data.timestamp || new Date().toISOString(),
    data.artistName || '',
    data.phoneNumber || '',
    data.hasBusinessNumber ? 'Y' : 'N',
    data.categories || '',
    data.qualificationStatus || '',
    data.interested2026Food ? 'Y' : 'N',
    data.interested2026Digital ? 'Y' : 'N',
    data.step1Completed ? 'Y' : 'N',
    data.step2Completed ? 'Y' : 'N',
    data.step3Completed ? 'Y' : 'N',
    data.quizCompleted ? 'Y' : 'N',
    data.quizScore || 0,
    data.registrationClicked ? 'Y' : 'N',
    data.utmSource || '',
    data.utmMedium || '',
    data.utmCampaign || ''
  ]);
}

/**
 * 2026 확장 대기 시트에 저장
 */
function saveTo2026WaitlistSheet(data) {
  const sheet = getOrCreateSheet('2026확장대기');
  
  // 헤더가 없으면 추가
  if (sheet.getLastRow() === 0) {
    sheet.appendRow(['타임스탬프', '작가명', '연락처', '관심카테고리', '비고']);
  }
  
  // 관심 카테고리 문자열 생성
  const interests = [];
  if (data.interested2026Food) interests.push('식품');
  if (data.interested2026Digital) interests.push('디지털작품');
  
  sheet.appendRow([
    data.timestamp || new Date().toISOString(),
    data.artistName || '',
    data.phoneNumber || '',
    interests.join(', '),
    '2026년 확장 시 연락 예정'
  ]);
}

/**
 * 사업자 미등록 시트에 저장
 */
function saveToNoBusinessSheet(data) {
  const sheet = getOrCreateSheet('사업자미등록');
  
  // 헤더가 없으면 추가
  if (sheet.getLastRow() === 0) {
    sheet.appendRow(['타임스탬프', '작가명', '연락처', '관심카테고리']);
  }
  
  sheet.appendRow([
    data.timestamp || new Date().toISOString(),
    data.artistName || '',
    data.phoneNumber || '',
    data.categories || ''
  ]);
}

/**
 * 시트 가져오기 또는 생성
 */
function getOrCreateSheet(sheetName) {
  const spreadsheet = SpreadsheetApp.getActiveSpreadsheet();
  let sheet = spreadsheet.getSheetByName(sheetName);
  
  if (!sheet) {
    sheet = spreadsheet.insertSheet(sheetName);
  }
  
  return sheet;
}

/**
 * JSON 응답 생성
 */
function createResponse(success, message) {
  const response = {
    success: success,
    message: message,
    timestamp: new Date().toISOString()
  };
  
  return ContentService
    .createTextOutput(JSON.stringify(response))
    .setMimeType(ContentService.MimeType.JSON);
}

/**
 * 학습 진행률 업데이트 (선택적)
 */
function updateLearningProgress(phoneNumber, progressData) {
  const sheet = getOrCreateSheet('전체데이터');
  const data = sheet.getDataRange().getValues();
  
  // 연락처로 행 찾기
  for (let i = 1; i < data.length; i++) {
    if (data[i][2] === phoneNumber) { // 연락처 열 (C열, 인덱스 2)
      // 진행률 업데이트
      if (progressData.step1Completed !== undefined) {
        sheet.getRange(i + 1, 9).setValue(progressData.step1Completed ? 'Y' : 'N');
      }
      if (progressData.step2Completed !== undefined) {
        sheet.getRange(i + 1, 10).setValue(progressData.step2Completed ? 'Y' : 'N');
      }
      if (progressData.step3Completed !== undefined) {
        sheet.getRange(i + 1, 11).setValue(progressData.step3Completed ? 'Y' : 'N');
      }
      if (progressData.quizCompleted !== undefined) {
        sheet.getRange(i + 1, 12).setValue(progressData.quizCompleted ? 'Y' : 'N');
        sheet.getRange(i + 1, 13).setValue(progressData.quizScore || 0);
      }
      if (progressData.registrationClicked !== undefined) {
        sheet.getRange(i + 1, 14).setValue(progressData.registrationClicked ? 'Y' : 'N');
      }
      break;
    }
  }
}

/**
 * 테스트 함수 - 스크립트 에디터에서 실행하여 테스트
 */
function testDoPost() {
  const testData = {
    postData: {
      contents: JSON.stringify({
        timestamp: new Date().toISOString(),
        artistName: '테스트작가',
        phoneNumber: '010-1234-5678',
        hasBusinessNumber: true,
        categories: '악세서리, 가방',
        qualificationStatus: 'qualified',
        interested2026Food: false,
        interested2026Digital: true,
        step1Completed: true,
        step2Completed: false,
        step3Completed: false,
        quizCompleted: false,
        quizScore: 0,
        registrationClicked: false
      })
    }
  };
  
  const result = doPost(testData);
  console.log(result.getContent());
}
```

### Step 4: Apps Script 배포

1. **저장** (Ctrl+S 또는 💾 아이콘)
2. **배포** → **새 배포** 클릭
3. 설정:
   - **유형 선택**: ⚙️ 톱니바퀴 → **웹 앱** 선택
   - **설명**: "글로벌 작가 온보딩 API v1.0"
   - **다음 사용자 인증 정보로 실행**: "나" 선택
   - **액세스 권한이 있는 사용자**: **"모든 사용자"** 선택 ⚠️ 중요!
4. **배포** 클릭
5. **액세스 승인** 클릭 → Google 계정 선택 → "고급" → "프로젝트명(안전하지 않음)으로 이동" → "허용"
6. **웹 앱 URL 복사** (아래와 같은 형식)
   ```
   https://script.google.com/macros/s/AKfycbx.../exec
   ```

### Step 5: 테스트

Apps Script 에디터에서:
1. 함수 선택: `testDoPost`
2. **실행** 클릭
3. 스프레드시트에서 데이터 확인

---

## 3. Vercel 배포

### 방법 1: Vercel 웹사이트에서 배포 (권장)

1. [Vercel](https://vercel.com) 접속 및 로그인 (GitHub 계정 연동)
2. **Add New...** → **Project** 클릭
3. **Import Git Repository**에서 `pos01204/artists-global-registration` 선택
4. **Configure Project**:
   - **Project Name**: `artists-global-registration` (또는 원하는 이름)
   - **Framework Preset**: `Next.js` (자동 감지됨)
   - **Root Directory**: `.` (기본값)
   - **Build Command**: `npm run build` (기본값)
   - **Output Directory**: `.next` (기본값)
5. **Environment Variables** 설정:
   ```
   GOOGLE_SCRIPT_URL = https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
   ```
6. **Deploy** 클릭
7. 배포 완료 후 URL 확인 (예: `https://artists-global-registration.vercel.app`)

### 방법 2: Vercel CLI로 배포

```bash
# 1. Vercel CLI 설치
npm install -g vercel

# 2. 프로젝트 폴더에서 로그인
cd "C:\Users\김지훈\Desktop\[Global Business셀] 김지훈\AI 자동화\글로벌 작가 온보딩 페이지"
vercel login

# 3. 배포 (처음 실행 시 설정 진행)
vercel

# 질문에 답변:
# - Set up and deploy? Y
# - Which scope? (본인 계정 선택)
# - Link to existing project? N
# - Project name? artists-global-registration
# - Directory? ./
# - Override settings? N

# 4. 프로덕션 배포
vercel --prod
```

### 커스텀 도메인 설정 (선택사항)

1. Vercel 대시보드 → 프로젝트 선택
2. **Settings** → **Domains**
3. 원하는 도메인 입력 (예: `global-artist.idus.com`)
4. DNS 설정 안내에 따라 설정

---

## 4. 환경변수 설정

### 로컬 개발 환경

프로젝트 루트에 `.env.local` 파일 생성:

```env
# 구글 앱스 스크립트 웹 앱 URL
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
```

### Vercel 환경변수

1. Vercel 대시보드 → 프로젝트 선택
2. **Settings** → **Environment Variables**
3. 추가:
   - **Name**: `GOOGLE_SCRIPT_URL`
   - **Value**: `https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec`
   - **Environment**: Production, Preview, Development 모두 체크
4. **Save** 클릭
5. **Deployments** → **Redeploy** (환경변수 적용)

---

## 5. 배포 후 확인사항

### 체크리스트

```
□ 1. GitHub 레포지토리에 코드 푸시 완료
□ 2. 구글 스프레드시트 생성 완료
□ 3. Apps Script 배포 완료
□ 4. Apps Script 테스트 성공
□ 5. Vercel 배포 완료
□ 6. 환경변수 설정 완료
□ 7. 배포된 사이트 접속 테스트
□ 8. 폼 제출 → 구글 시트 저장 테스트
□ 9. 학습 플로우 전체 테스트
□ 10. 모바일 반응형 테스트
```

### 테스트 시나리오

1. **자격 충족 케이스**
   - 사업자등록번호 있음 + 판매 가능 카테고리 선택
   - → 학습 과정 진행 → 퀴즈 완료 → 완료 페이지

2. **사업자 미등록 케이스**
   - 사업자등록번호 없음 선택
   - → 사업자 미등록 안내 페이지

3. **2026 대기 케이스**
   - 식품 또는 디지털 카테고리만 선택
   - → 2026 대기 리스트 안내 페이지

### 문제 해결

| 문제 | 해결 방법 |
|------|----------|
| 구글 시트에 데이터 안 들어감 | Apps Script 배포 권한 확인, URL 정확히 복사 |
| CORS 오류 | Apps Script에서 "모든 사용자" 액세스 권한 확인 |
| Vercel 빌드 실패 | `npm run build` 로컬에서 먼저 테스트 |
| 환경변수 적용 안 됨 | Vercel에서 Redeploy 필요 |

---

## 📞 지원

- **idus 채널톡**: https://idus.channel.io
- **상담시간**: 평일 오전 10시 ~ 오후 6시

---

**작성일**: 2024-12-23  
**버전**: v1.0

