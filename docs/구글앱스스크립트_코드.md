# 📊 구글 앱스 스크립트 코드 - 상세 가이드

## 📋 개요

이 문서는 글로벌 작가 온보딩 페이지에서 수집된 데이터를 구글 스프레드시트에 저장하기 위한 Apps Script 코드와 설정 방법을 상세히 안내합니다.

---

## 1. 스프레드시트 준비

### 1.1 새 스프레드시트 생성

1. [Google Sheets](https://sheets.google.com) 접속
2. **+ 새 스프레드시트** 클릭
3. 제목 변경: `글로벌 작가 온보딩 데이터`

### 1.2 시트 구조

**3개의 시트를 생성합니다:**

#### Sheet 1: "전체데이터"
> 모든 온보딩 참여자 데이터

| 열 | 헤더명 | 설명 |
|----|--------|------|
| A | 타임스탬프 | 데이터 저장 시간 |
| B | 작가명 | 작가명/브랜드명 |
| C | 연락처 | 휴대폰 번호 |
| D | 사업자보유 | Y/N |
| E | 카테고리 | 선택한 카테고리 |
| F | 자격상태 | qualified/no_business/restricted_category |
| G | 식품관심 | Y/N |
| H | 디지털관심 | Y/N |
| I | STEP1완료 | Y/N |
| J | STEP2완료 | Y/N |
| K | STEP3완료 | Y/N |
| L | 퀴즈완료 | Y/N |
| M | 퀴즈점수 | 0-3 |
| N | 등록클릭 | Y/N |
| O | UTM_SOURCE | 유입 소스 |
| P | UTM_MEDIUM | 유입 매체 |
| Q | UTM_CAMPAIGN | 캠페인명 |

#### Sheet 2: "2026확장대기"
> 식품/디지털 카테고리 관심자 별도 관리

| 열 | 헤더명 | 설명 |
|----|--------|------|
| A | 타임스탬프 | 데이터 저장 시간 |
| B | 작가명 | 작가명/브랜드명 |
| C | 연락처 | 휴대폰 번호 |
| D | 관심카테고리 | 식품, 디지털작품 |
| E | 비고 | 메모 |

#### Sheet 3: "사업자미등록"
> 사업자등록번호 미보유자 (리마케팅용)

| 열 | 헤더명 | 설명 |
|----|--------|------|
| A | 타임스탬프 | 데이터 저장 시간 |
| B | 작가명 | 작가명/브랜드명 |
| C | 연락처 | 휴대폰 번호 |
| D | 관심카테고리 | 선택한 카테고리 |

---

## 2. Apps Script 설정

### 2.1 Apps Script 열기

1. 스프레드시트 상단 메뉴: **확장 프로그램** → **Apps Script**
2. 새 탭에서 Apps Script 에디터가 열림
3. 기본 코드 (`function myFunction() {}`) 전체 삭제

### 2.2 전체 코드

아래 코드를 복사하여 붙여넣기:

```javascript
/**
 * ============================================================
 * 글로벌 작가 온보딩 데이터 수집 스크립트
 * ============================================================
 * 
 * 버전: 1.0.0
 * 작성일: 2024-12-23
 * 
 * 기능:
 * - POST 요청으로 온보딩 데이터 수신
 * - 자격 상태에 따라 다른 시트에 저장
 * - 2026 확장 카테고리 관심자 별도 관리
 * 
 * 시트 구조:
 * - 전체데이터: 모든 온보딩 데이터
 * - 2026확장대기: 식품/디지털 카테고리 관심자
 * - 사업자미등록: 사업자등록번호 미보유자
 */

// ============================================================
// 설정
// ============================================================

const CONFIG = {
  SHEETS: {
    MAIN: '전체데이터',
    WAITLIST_2026: '2026확장대기',
    NO_BUSINESS: '사업자미등록'
  },
  HEADERS: {
    MAIN: [
      '타임스탬프', '작가명', '연락처', '사업자보유', '카테고리',
      '자격상태', '식품관심', '디지털관심', 'STEP1완료', 'STEP2완료',
      'STEP3완료', '퀴즈완료', '퀴즈점수', '등록클릭', 'UTM_SOURCE',
      'UTM_MEDIUM', 'UTM_CAMPAIGN'
    ],
    WAITLIST_2026: ['타임스탬프', '작가명', '연락처', '관심카테고리', '비고'],
    NO_BUSINESS: ['타임스탬프', '작가명', '연락처', '관심카테고리']
  }
};

// ============================================================
// 메인 핸들러
// ============================================================

/**
 * POST 요청 처리 - 온보딩 데이터 저장
 * @param {Object} e - 요청 이벤트 객체
 * @returns {TextOutput} JSON 응답
 */
function doPost(e) {
  try {
    // CORS 프리플라이트 처리
    if (e.parameter && e.parameter.preflight === 'true') {
      return createCorsResponse();
    }
    
    // 요청 데이터 파싱
    const data = JSON.parse(e.postData.contents);
    
    // 데이터 검증
    const validation = validateData(data);
    if (!validation.valid) {
      return createResponse(false, validation.message);
    }
    
    // 타임스탬프 추가
    data.timestamp = data.timestamp || new Date().toISOString();
    
    // 메인 시트에 저장
    saveToMainSheet(data);
    
    // 자격 상태에 따라 추가 시트에 저장
    if (data.qualificationStatus === 'no_business') {
      saveToNoBusinessSheet(data);
    }
    
    // 2026 확장 카테고리 관심자 저장
    if (data.interested2026Food || data.interested2026Digital) {
      saveTo2026WaitlistSheet(data);
    }
    
    // 로그 기록
    console.log('Data saved successfully:', data.artistName);
    
    return createResponse(true, '데이터가 성공적으로 저장되었습니다.');
    
  } catch (error) {
    console.error('Error in doPost:', error);
    return createResponse(false, '서버 오류: ' + error.message);
  }
}

/**
 * GET 요청 처리 - 헬스체크 및 테스트
 * @param {Object} e - 요청 이벤트 객체
 * @returns {TextOutput} JSON 응답
 */
function doGet(e) {
  return createResponse(true, 'Global Artist Onboarding API is running', {
    version: '1.0.0',
    timestamp: new Date().toISOString()
  });
}

// ============================================================
// 데이터 저장 함수
// ============================================================

/**
 * 메인 시트에 데이터 저장
 * @param {Object} data - 온보딩 데이터
 */
function saveToMainSheet(data) {
  const sheet = getOrCreateSheet(CONFIG.SHEETS.MAIN, CONFIG.HEADERS.MAIN);
  
  const row = [
    data.timestamp,
    data.artistName || '',
    data.phoneNumber || '',
    boolToYN(data.hasBusinessNumber),
    data.categories || '',
    data.qualificationStatus || '',
    boolToYN(data.interested2026Food),
    boolToYN(data.interested2026Digital),
    boolToYN(data.step1Completed),
    boolToYN(data.step2Completed),
    boolToYN(data.step3Completed),
    boolToYN(data.quizCompleted),
    data.quizScore || 0,
    boolToYN(data.registrationClicked),
    data.utmSource || '',
    data.utmMedium || '',
    data.utmCampaign || ''
  ];
  
  sheet.appendRow(row);
}

/**
 * 2026 확장 대기 시트에 저장
 * @param {Object} data - 온보딩 데이터
 */
function saveTo2026WaitlistSheet(data) {
  const sheet = getOrCreateSheet(CONFIG.SHEETS.WAITLIST_2026, CONFIG.HEADERS.WAITLIST_2026);
  
  // 관심 카테고리 문자열 생성
  const interests = [];
  if (data.interested2026Food) interests.push('식품');
  if (data.interested2026Digital) interests.push('디지털작품');
  
  const row = [
    data.timestamp,
    data.artistName || '',
    data.phoneNumber || '',
    interests.join(', '),
    '2026년 확장 시 연락 예정'
  ];
  
  sheet.appendRow(row);
}

/**
 * 사업자 미등록 시트에 저장
 * @param {Object} data - 온보딩 데이터
 */
function saveToNoBusinessSheet(data) {
  const sheet = getOrCreateSheet(CONFIG.SHEETS.NO_BUSINESS, CONFIG.HEADERS.NO_BUSINESS);
  
  const row = [
    data.timestamp,
    data.artistName || '',
    data.phoneNumber || '',
    data.categories || ''
  ];
  
  sheet.appendRow(row);
}

// ============================================================
// 학습 진행률 업데이트 (선택적 기능)
// ============================================================

/**
 * 기존 데이터의 학습 진행률 업데이트
 * @param {string} phoneNumber - 연락처 (식별자)
 * @param {Object} progressData - 진행률 데이터
 */
function updateLearningProgress(phoneNumber, progressData) {
  const sheet = getOrCreateSheet(CONFIG.SHEETS.MAIN, CONFIG.HEADERS.MAIN);
  const data = sheet.getDataRange().getValues();
  
  // 연락처로 행 찾기 (C열, 인덱스 2)
  for (let i = 1; i < data.length; i++) {
    if (data[i][2] === phoneNumber) {
      const rowNum = i + 1;
      
      // 진행률 업데이트
      if (progressData.step1Completed !== undefined) {
        sheet.getRange(rowNum, 9).setValue(boolToYN(progressData.step1Completed));
      }
      if (progressData.step2Completed !== undefined) {
        sheet.getRange(rowNum, 10).setValue(boolToYN(progressData.step2Completed));
      }
      if (progressData.step3Completed !== undefined) {
        sheet.getRange(rowNum, 11).setValue(boolToYN(progressData.step3Completed));
      }
      if (progressData.quizCompleted !== undefined) {
        sheet.getRange(rowNum, 12).setValue(boolToYN(progressData.quizCompleted));
        sheet.getRange(rowNum, 13).setValue(progressData.quizScore || 0);
      }
      if (progressData.registrationClicked !== undefined) {
        sheet.getRange(rowNum, 14).setValue(boolToYN(progressData.registrationClicked));
      }
      
      console.log('Progress updated for:', phoneNumber);
      break;
    }
  }
}

// ============================================================
// 유틸리티 함수
// ============================================================

/**
 * 시트 가져오기 또는 생성
 * @param {string} sheetName - 시트 이름
 * @param {Array} headers - 헤더 배열
 * @returns {Sheet} 시트 객체
 */
function getOrCreateSheet(sheetName, headers) {
  const spreadsheet = SpreadsheetApp.getActiveSpreadsheet();
  let sheet = spreadsheet.getSheetByName(sheetName);
  
  if (!sheet) {
    sheet = spreadsheet.insertSheet(sheetName);
    if (headers && headers.length > 0) {
      sheet.appendRow(headers);
      // 헤더 행 스타일링
      sheet.getRange(1, 1, 1, headers.length)
        .setBackground('#FF6F00')
        .setFontColor('#FFFFFF')
        .setFontWeight('bold');
      sheet.setFrozenRows(1);
    }
  } else if (sheet.getLastRow() === 0 && headers) {
    sheet.appendRow(headers);
    sheet.getRange(1, 1, 1, headers.length)
      .setBackground('#FF6F00')
      .setFontColor('#FFFFFF')
      .setFontWeight('bold');
    sheet.setFrozenRows(1);
  }
  
  return sheet;
}

/**
 * Boolean을 Y/N 문자열로 변환
 * @param {boolean} value - Boolean 값
 * @returns {string} 'Y' 또는 'N'
 */
function boolToYN(value) {
  return value ? 'Y' : 'N';
}

/**
 * 데이터 검증
 * @param {Object} data - 검증할 데이터
 * @returns {Object} { valid: boolean, message: string }
 */
function validateData(data) {
  if (!data) {
    return { valid: false, message: '데이터가 없습니다.' };
  }
  if (!data.artistName || data.artistName.trim() === '') {
    return { valid: false, message: '작가명이 필요합니다.' };
  }
  if (!data.phoneNumber || data.phoneNumber.trim() === '') {
    return { valid: false, message: '연락처가 필요합니다.' };
  }
  return { valid: true, message: 'OK' };
}

/**
 * JSON 응답 생성
 * @param {boolean} success - 성공 여부
 * @param {string} message - 메시지
 * @param {Object} additionalData - 추가 데이터
 * @returns {TextOutput} JSON 응답
 */
function createResponse(success, message, additionalData = {}) {
  const response = {
    success: success,
    message: message,
    timestamp: new Date().toISOString(),
    ...additionalData
  };
  
  return ContentService
    .createTextOutput(JSON.stringify(response))
    .setMimeType(ContentService.MimeType.JSON);
}

/**
 * CORS 프리플라이트 응답 생성
 * @returns {TextOutput} 빈 응답
 */
function createCorsResponse() {
  return ContentService
    .createTextOutput('')
    .setMimeType(ContentService.MimeType.TEXT);
}

// ============================================================
// 테스트 함수
// ============================================================

/**
 * POST 요청 테스트 - 에디터에서 실행
 */
function testDoPost() {
  const testData = {
    postData: {
      contents: JSON.stringify({
        timestamp: new Date().toISOString(),
        artistName: '테스트작가_' + Date.now(),
        phoneNumber: '010-1234-5678',
        hasBusinessNumber: true,
        categories: '악세서리, 가방',
        qualificationStatus: 'qualified',
        interested2026Food: false,
        interested2026Digital: true,
        step1Completed: true,
        step2Completed: true,
        step3Completed: false,
        quizCompleted: false,
        quizScore: 0,
        registrationClicked: false,
        utmSource: 'test',
        utmMedium: 'direct',
        utmCampaign: 'test_campaign'
      })
    }
  };
  
  const result = doPost(testData);
  console.log('Test Result:', result.getContent());
}

/**
 * 사업자 미등록 케이스 테스트
 */
function testNoBusinessCase() {
  const testData = {
    postData: {
      contents: JSON.stringify({
        timestamp: new Date().toISOString(),
        artistName: '사업자미등록_테스트',
        phoneNumber: '010-9999-8888',
        hasBusinessNumber: false,
        categories: '문구, 캔들',
        qualificationStatus: 'no_business',
        interested2026Food: false,
        interested2026Digital: false
      })
    }
  };
  
  const result = doPost(testData);
  console.log('No Business Test Result:', result.getContent());
}

/**
 * 2026 대기 케이스 테스트
 */
function test2026WaitlistCase() {
  const testData = {
    postData: {
      contents: JSON.stringify({
        timestamp: new Date().toISOString(),
        artistName: '2026대기_테스트',
        phoneNumber: '010-7777-6666',
        hasBusinessNumber: true,
        categories: '',
        qualificationStatus: 'restricted_category',
        interested2026Food: true,
        interested2026Digital: true
      })
    }
  };
  
  const result = doPost(testData);
  console.log('2026 Waitlist Test Result:', result.getContent());
}

/**
 * GET 요청 테스트
 */
function testDoGet() {
  const result = doGet({});
  console.log('GET Test Result:', result.getContent());
}

/**
 * 모든 시트 초기화 (개발용 - 주의해서 사용)
 */
function resetAllSheets() {
  const spreadsheet = SpreadsheetApp.getActiveSpreadsheet();
  
  Object.values(CONFIG.SHEETS).forEach(sheetName => {
    const sheet = spreadsheet.getSheetByName(sheetName);
    if (sheet) {
      spreadsheet.deleteSheet(sheet);
    }
  });
  
  console.log('All sheets have been reset.');
}
```

### 2.3 코드 저장

- **Ctrl + S** 또는 💾 아이콘 클릭
- 프로젝트 이름 설정: `글로벌작가온보딩API`

---

## 3. 배포

### 3.1 새 배포 생성

1. 상단 메뉴: **배포** → **새 배포**
2. ⚙️ 톱니바퀴 클릭 → **웹 앱** 선택

### 3.2 배포 설정

| 설정 | 값 |
|------|-----|
| 설명 | 글로벌 작가 온보딩 API v1.0 |
| 다음 사용자 인증 정보로 실행 | **나** |
| 액세스 권한이 있는 사용자 | **모든 사용자** ⚠️ 중요! |

### 3.3 권한 승인

1. **배포** 클릭
2. **액세스 승인** 클릭
3. Google 계정 선택
4. "고급" 클릭
5. "프로젝트명(안전하지 않음)으로 이동" 클릭
6. **허용** 클릭

### 3.4 URL 복사

배포 완료 후 표시되는 **웹 앱 URL** 복사:

```
https://script.google.com/macros/s/AKfycby.../exec
```

⚠️ 이 URL을 안전하게 보관하세요!

---

## 4. 테스트

### 4.1 에디터에서 테스트

1. 함수 선택 드롭다운에서 `testDoPost` 선택
2. **실행** 버튼 클릭
3. 첫 실행 시 권한 승인 필요
4. 실행 로그에서 결과 확인
5. 스프레드시트에서 데이터 확인

### 4.2 추가 테스트

- `testNoBusinessCase`: 사업자 미등록 케이스
- `test2026WaitlistCase`: 2026 대기 리스트 케이스
- `testDoGet`: GET 요청 (헬스체크)

### 4.3 외부에서 테스트 (선택)

```bash
# curl로 테스트 (터미널/명령 프롬프트)
curl -X POST \
  -H "Content-Type: application/json" \
  -d '{"artistName":"테스트","phoneNumber":"010-1234-5678","hasBusinessNumber":true,"categories":"악세서리","qualificationStatus":"qualified"}' \
  "https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec"
```

---

## 5. 문제 해결

### 5.1 자주 발생하는 오류

| 오류 | 원인 | 해결 방법 |
|------|------|----------|
| 권한 오류 | 액세스 권한 설정 | "모든 사용자"로 변경 |
| CORS 오류 | 브라우저 보안 정책 | 웹 앱 배포 확인 |
| 데이터 안 들어감 | URL 오류 | 배포 URL 정확히 복사 |
| 스크립트 오류 | 코드 문법 오류 | 로그 확인 후 수정 |

### 5.2 로그 확인

1. Apps Script 에디터 → **실행** 메뉴 → **실행 기록**
2. 또는 **보기** → **로그**

### 5.3 배포 업데이트

코드 수정 후:
1. **배포** → **배포 관리**
2. ✏️ 연필 아이콘 클릭
3. **버전**: "새 버전" 선택
4. **배포** 클릭

---

## 6. 보안 고려사항

1. **URL 노출 주의**: 웹 앱 URL은 공개되면 누구나 데이터를 보낼 수 있음
2. **데이터 검증**: 서버 측에서 항상 데이터 검증 수행
3. **접근 제한**: 필요시 특정 도메인에서만 요청 허용하도록 수정 가능
4. **정기 백업**: 스프레드시트 정기적으로 백업

---

**작성일**: 2024-12-23  
**버전**: 1.0.0

