# 🌏 글로벌 작가 온보딩 페이지

idus 글로벌 작가 등록을 위한 온보딩 페이지입니다.

## 🎯 프로젝트 목적

KR 서버에 입점된 작가님들이 글로벌 판매에 대한 어려움을 낮추고, 재미있고 쉬운 온보딩을 통해 실제 글로벌 작가 등록까지 이어지도록 유도합니다.

## ✨ 주요 기능

### 1. 작가 정보 입력 & 자격 확인
- 작가명/브랜드명, 연락처 수집
- 사업자등록번호 보유 여부 확인
- 판매 카테고리 선택
- 2026년 확장 예정 카테고리(식품/디지털) 관심 수집

### 2. 자격 분기 처리
- ✅ **자격 충족**: 학습 과정으로 진행
- ⚠️ **사업자 미등록**: 안내 페이지 표시 (리마케팅용 데이터 저장)
- 📋 **2026 대기**: 대기 리스트 등록 안내

### 3. 3단계 학습 과정
- **STEP 1**: 글로벌 서비스 이해하기 (~15분)
- **STEP 2**: 작품 등록 마스터하기 (~20분)
- **STEP 3**: 주문 처리 & 운영하기 (~15분)
- **퀴즈**: 학습 내용 확인 (3문제)

### 4. 완료 & 보상
- 학습 완료 축하 페이지
- KR 광고포인트 10,000P 지급 안내
- 글로벌 작가 등록 유도 CTA

### 5. 데이터 수집
- 구글 시트 자동 저장
- UTM 파라미터 추적

## 🛠️ 기술 스택

- **Framework**: Next.js 14 (App Router)
- **Styling**: Tailwind CSS
- **Language**: TypeScript
- **Animation**: Framer Motion (선택적)
- **Icons**: Lucide React
- **Deployment**: Vercel

## 📁 프로젝트 구조

```
글로벌-작가-온보딩/
├── public/
│   ├── brand/                    # 브랜드 에셋
│   ├── fonts/                    # SUITE 폰트
│   └── loading/                  # 로딩 애니메이션
├── src/
│   ├── app/
│   │   ├── page.tsx              # 랜딩 (작가 정보 입력)
│   │   ├── qualification/
│   │   │   ├── no-business/      # 사업자 미등록 안내
│   │   │   └── 2026-waitlist/    # 2026 대기 안내
│   │   ├── learn/
│   │   │   ├── page.tsx          # 학습 메인
│   │   │   ├── step/[stepId]/    # STEP별 학습
│   │   │   └── quiz/             # 퀴즈
│   │   ├── complete/             # 완료 & 보상
│   │   └── api/
│   │       └── submit/           # 데이터 제출 API
│   ├── components/
│   │   ├── ui/                   # 공통 UI 컴포넌트
│   │   └── forms/                # 폼 컴포넌트
│   ├── lib/
│   │   ├── storage.ts            # localStorage 관리
│   │   └── api.ts                # API 유틸리티
│   ├── data/
│   │   └── contents.ts           # 학습 콘텐츠 데이터
│   └── types/
│       └── onboarding.ts         # 타입 정의
└── docs/                         # 기획 문서
```

## 🚀 시작하기

### 1. 의존성 설치

```bash
npm install
```

### 2. 개발 서버 실행

```bash
npm run dev
```

### 3. 빌드

```bash
npm run build
```

### 4. 프로덕션 실행

```bash
npm start
```

## ⚙️ 환경 변수 설정

`env.example`을 참고하여 `.env.local` 파일을 생성하세요.

```env
# 구글 시트 연동
GOOGLE_SCRIPT_URL=https://script.google.com/macros/s/YOUR_SCRIPT_ID/exec
```

## 📊 구글 시트 연동

### Google Apps Script 설정

1. Google Sheets에서 새 스프레드시트 생성
2. 확장 프로그램 → Apps Script
3. 아래 코드 추가:

```javascript
function doPost(e) {
  const sheet = SpreadsheetApp.getActiveSpreadsheet().getActiveSheet();
  const data = JSON.parse(e.postData.contents);
  
  sheet.appendRow([
    data.timestamp,
    data.artistName,
    data.phoneNumber,
    data.hasBusinessNumber,
    data.categories,
    data.qualificationStatus,
    data.interested2026Food,
    data.interested2026Digital,
    // ... 추가 필드
  ]);
  
  return ContentService.createTextOutput(JSON.stringify({success: true}))
    .setMimeType(ContentService.MimeType.JSON);
}
```

4. 배포 → 새 배포 → 웹 앱으로 배포
5. 배포 URL을 환경변수에 설정

## 🎨 디자인 시스템

### 컬러
- **Primary Orange**: #FF6F00
- **Primary Black**: #212121
- **Gray**: #F5F5F5

### 폰트
- **SUITE Variable**: 메인 폰트

## 📝 라이선스

© 2024 Backpackr Inc. All rights reserved.

