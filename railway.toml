[build]
builder = "nixpacks"
watchPatterns = ["backend/**"]

[build.nixpacksPlan.phases.setup]
nixPkgs = ["nodejs_20"]

[deploy]
startCommand = "cd backend && npm install --omit=dev && npm run start"


